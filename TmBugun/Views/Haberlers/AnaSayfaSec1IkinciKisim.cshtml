@model IEnumerable<TmBugun.Models.Haber>



    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-12">
                @{
                    var base64 = Convert.ToBase64String(item.HaberResim);
                    string imgsrc = String.Format("data:image/jpeg;base64,{0}", base64);
                }
                <div style="margin-bottom:20px;">
                    <a href="/Haberlers/HaberDetay/@item.Id" style="color:white">

                        <div class="card card-image view overlay img-fluid" style="background-image:url(@imgsrc);">
                            <div class="mask waves-effect waves-light rgba-white-slight"></div>
                            <!-- Content -->
                            <div class="text-white text-md-left d-flex align-items-start resimUzeriYazi" style="padding:15px;padding-top:20%">
                                <div>
                                    <h3 class="card-title pt-2" style="font-size:16px;">
                                        <strong>
                                            @Html.Raw(item.HaberBaslik.ToString().Substring(0, (item.HaberBaslik.Length > 50) ? 50 : item.HaberBaslik.Length))
                                        </strong>
                                    </h3>
                                    <h6 class="white-text">
                                        | @Html.DisplayFor(modelItem => item.HaberKategorileri.HaberKategori)
                                    </h6>
                                </div>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
            @*<div class="col-md-2 col-sm-12" style="height:20%;padding:0 7px 0 7px">
                <div class="card card-cascade">
                    <a href="~/Haberlers/HaberDetay/@item.Id" style="color:white">
                        <!-- Card image -->
                        <div class="view view-cascade overlay">
                            @Html.Raw("<img style='width:100%; height:120px;' src=\"data:image/jpeg;base64,"
                                + Convert.ToBase64String(item.HaberResim) + "\" />")
                        </div>
                        <!-- Card content -->
                        <div class="card-body card-body-cascade text-left">
                            <!-- Title -->
                            <!-- Subtitle -->
                            <h6 class="font-weight-bold indigo-text text-left" style="margin:0;font-size:14px;color:black;">
                                @Html.Raw(item.HaberBaslik)
                            </h6>
                            <!-- Text -->
                            <p class="card-text text-left" style="font-size:14px;color:black;">
                                @Html.Raw(item.HaberIcerik.Substring(0,100))
                            </p>
                        </div>
                        <!-- Card footer -->
                        <div class="card-footer text-muted text-left" style="font-size:14px;">
                            @Html.Raw(item.HaberTarih.ToShortDateString())
                            @Html.Raw(item.HaberTarih.ToShortTimeString())                     |
                            @Html.DisplayFor(modelItem => item.HaberKategorileri.HaberKategori)
                        </div>
                    </a>
                </div>
            </div>
            *@
        }
    </div>
